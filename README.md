# InConti Health Care static website

Upload `index.html` to the root of a GitHub repository and enable GitHub Pages.

The site is self-contained and requires no build step, backend, database, or external CSS/JS dependencies.
